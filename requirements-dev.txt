-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
# Pinned exactly so a new ruff release cannot expand the default ruleset and
# turn CI red without a code change. Dependabot proposes bumps as PRs.
ruff==0.16.4
